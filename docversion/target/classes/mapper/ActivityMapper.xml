<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.docversion.mapper.ActivityMapper">

    <!-- logDocumentChangeHistory: 활동 이력 INSERT.
         `timestamp`, `user`는 MariaDB 예약어 → 백틱. app은 'files' 고정. -->
    <insert id="insertActivity">
        INSERT INTO activity
            (`timestamp`, `user`, affecteduser, app,
             subject, subjectparams, file, object_type, object_id)
        VALUES
            (#{timestamp}, #{user}, #{affecteduser}, 'files',
             #{subject}, #{subjectparams}, #{file}, #{objectType}, #{objectId})
    </insert>

</mapper>
