<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.docversion.mapper.NotificationMapper">

    <!-- ============ 알림 ============ -->
    <insert id="insertNotificationIgnore">
        INSERT IGNORE INTO notifications
            (notification_id, `user`, `timestamp`, object_type, object_id, subject, message, dedup_key)
        VALUES
            (#{notificationId}, #{user}, #{timestamp}, #{objectType}, #{objectId}, #{subject}, #{message}, #{dedupKey})
    </insert>

    <select id="listByUser" resultType="map">
        SELECT notification_id AS notificationId, `user`, `timestamp`,
               object_type AS objectType, object_id AS objectId,
               subject, message, read_at AS readAt
        FROM notifications
        WHERE `user` = #{user}
        <if test="unreadOnly"> AND read_at IS NULL </if>
        ORDER BY `timestamp` DESC, notification_id DESC
        LIMIT #{limit}
    </select>

    <select id="countUnread" resultType="int">
        SELECT COUNT(*) FROM notifications WHERE `user` = #{user} AND read_at IS NULL
    </select>

    <update id="markRead">
        UPDATE notifications
        SET read_at = #{readAt}
        WHERE notification_id = #{notificationId} AND `user` = #{user} AND read_at IS NULL
    </update>

    <!-- ============ 구독 ============ -->
    <insert id="subscribeIgnore">
        INSERT IGNORE INTO file_subscriptions (file_id, user_id, created_at)
        VALUES (#{fileId}, #{userId}, #{createdAt})
    </insert>

    <delete id="unsubscribe">
        DELETE FROM file_subscriptions WHERE file_id = #{fileId} AND user_id = #{userId}
    </delete>

    <select id="listSubscribers" resultType="string">
        SELECT user_id FROM file_subscriptions WHERE file_id = #{fileId}
    </select>

    <!-- ============ 아웃박스 ============ -->
    <insert id="insertOutbox">
        INSERT INTO notification_outbox
            (notification_id, user_id, channel, payload, status, retry_count, retry_after, created_at)
        VALUES
            (#{notificationId}, #{userId}, #{channel}, #{payload}, 'PENDING', 0, #{retryAfter}, #{createdAt})
    </insert>

    <select id="findSendable" resultType="map">
        SELECT id, notification_id AS notificationId, user_id AS userId,
               channel, payload, retry_count AS retryCount
        FROM notification_outbox
        WHERE status = 'PENDING' AND retry_after &lt;= #{now}
        ORDER BY id
        LIMIT #{limit}
    </select>

    <!-- claim: PENDING인 것만 PROCESSING으로. 동시 워커 안전. -->
    <update id="claim">
        UPDATE notification_outbox
        SET status = 'PROCESSING', locked_by = #{workerId}, locked_at = #{lockedAt}
        WHERE id = #{id} AND status = 'PENDING'
    </update>

    <update id="markSent">
        UPDATE notification_outbox
        SET status = 'SENT', sent_at = #{sentAt}, locked_by = NULL, locked_at = NULL
        WHERE id = #{id}
    </update>

    <update id="markRetry">
        UPDATE notification_outbox
        SET status = 'PENDING', retry_count = #{retryCount}, retry_after = #{retryAfter},
            last_error = #{lastError}, locked_by = NULL, locked_at = NULL
        WHERE id = #{id}
    </update>

    <update id="markDlq">
        UPDATE notification_outbox
        SET status = 'DLQ', last_error = #{lastError}, locked_by = NULL, locked_at = NULL
        WHERE id = #{id}
    </update>

    <select id="listOutbox" resultType="map">
        SELECT id, user_id AS userId, channel, status, retry_count AS retryCount,
               payload, created_at AS createdAt, sent_at AS sentAt
        FROM notification_outbox
        ORDER BY id DESC
        LIMIT #{limit}
    </select>

</mapper>
